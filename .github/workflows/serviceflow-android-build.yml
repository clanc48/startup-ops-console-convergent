name: ServiceFlow Android Pilot APK

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/serviceflow-android-build.yml'
      - 'android-app/**'

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Runner smoke test
        run: |
          echo "GitHub-hosted runner is available"
          java -version
          uname -a
